# Only used by `npm run register`, which pushes the slash command definitions
# to your guild from Node. The Worker itself reads none of this — its config
# lives in wrangler.toml [vars] and in `wrangler secret put`.
DISCORD_TOKEN=
DISCORD_APP_ID=
DISCORD_GUILD_ID=
